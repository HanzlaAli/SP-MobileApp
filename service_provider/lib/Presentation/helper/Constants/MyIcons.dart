// ignore_for_file: file_names
import 'package:flutter/material.dart';

import 'MyColors.dart';

const byDefaultImage = Icon(
  Icons.person,
  color: kWhiteColor,
  size: 60,
);
const plusIcon = Icon(
  Icons.add,
  color: kBlackColor38,
);
const passwordShowIcon = Icon(
  Icons.visibility,
  color: kPrimaryColor,
);
const passwordHideIcon = Icon(
  Icons.visibility_off,
  color: kPrimaryColor,
);
